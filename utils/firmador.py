# utils/firmador.py
from lxml import etree

def firmar_xml(xml_string, cert_path):
    # ⚠ Esta función debe integrar un firmador XML real (ejemplo: signxml, PyXMLSecurity, etc.)
    # Por ahora solo retornamos el XML sin firmar como simulación
    return xml_string
